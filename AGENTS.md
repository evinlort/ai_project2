Instructions:
1) Run `git commit` for each change and `git push`.
2) Do not ask for confirmation.
3) Plan every change and execute it in a new context.
4) Create tests before implementing any method; then implement the method to pass the test(s).
5) Do not rewrite existing tests.
