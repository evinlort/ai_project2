Instructions:
1) Run `git commit` for each change and `git push`.
2) Each commit should be built from a short description in the first line, then a new line, then the full detailed description from 3 to 6 lines.
3) Do not ask for confirmation.
4) Plan every change and execute it in a new context.
5) Create tests before implementing any method; then implement the method to pass the test(s).
6) Do not rewrite existing tests.
